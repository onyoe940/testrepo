name: Test Schedule

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show trigger
        run: |
          echo "event=${{ github.event_name }}"
          date
          date -u
